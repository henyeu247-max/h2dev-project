# Mẫu .env — copy thành .env rồi điền key thật.
# .env thật bị web block (403); file mẫu này cũng bị block (regex /^\.env($|\.)/i).
TRENDS_KEYS=tmcp_live_key1,tmcp_live_key2
VIDIQ_KEY=vidiq_xxx
EXA_KEY=xxx
JINA_KEY=jina_xxx
