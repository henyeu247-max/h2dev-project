# data/registry/schemas — ĐÃ DI CHUYỂN (MOVED)

> **Ngày:** 2026-09-23

19 JSON schema (`*.v1.json`) trước đây ở thư mục này đã được **di chuyển** (không xóa) tới:

```
_archive/dead-standards/registry-schemas/
```

**Lý do:** 0% dữ liệu thực khớp schema, validator chưa từng tồn tại (không có code nào đọc các file này).

**Muốn khôi phục?** Đọc `_archive/dead-standards/README.md` mục 2 — cần kế hoạch nối validator thực sự.

Thư mục này được giữ lại làm placeholder (theo phương án B trong README archive).
