# A5 bounded fixtures

`mini-records.json` is synthetic-only input for status, duplicate-ID, source
path-collision, and empty-reference contract tests. It is not production data,
does not authorize an import, and intentionally contains no media/transcript
content.
